{
  "name": "logger-colors",
  "version": "1.0.10",
  "description": "Print logs with color and save them to a file at the same time",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Luis E. Gonzalez R.",
  "license": "ISC",
  "dependencies": {
    "center-align": "^1.0.1",
    "moment": "^2.24.0",
    "moment-timezone": "^0.5.25"
  },
  "publishConfig": {
    "registry": "http://nexus-0.dev.openshift.multivaloresgf.local:8081/repository/npm-hosted/"
  }
}
